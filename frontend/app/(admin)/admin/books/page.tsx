export default function AdminBooksPage() {
  return (
    <div>
      <h1 className="font-display text-2xl md:text-3xl font-semibold text-text-primary mb-4">Books</h1>
      <div className="p-10 text-center rounded-card border-2 border-dashed border-border text-text-secondary font-body">
        Admin Books management — coming in Phase 1 when the backend is wired.
      </div>
    </div>
  );
}
